---
type: bootstrap-record
created: 2026-09-15
last_updated: 1405-06-24
status: done
---

# 🛰️ گزارش Bootstrap (فاز ۰ تا ۲ مأموریت)

این سند رویداد bootstrap پروژه را ثبت می‌کند تا agentهای بعدی بدانند ساختار اولیه چگونه و چرا ساخته شد.

---

## ۱. مأموریت bootstrap

| بند | شرح |
|------|------|
| **فاز ۱** | مطالعه پروژه مرجع `Market-Research` (۱۴ فایل به ترتیب مأموریت) و استخراج روش‌شناسی و درس‌آموخته‌ها |
| **فاز ۲** | ساخت ساختار Vault مشابه مرجع در `Russia-market` با تطبیق برای بازار روسیه |
| **فاز ۳** | پرسش از کاربر درباره موضوع دقیق تحلیل (پس از این کامیت، با ابزار پرسش ساختاریافته) |

## ۲. آنچه از مرجع الگوبرداری شد

- ساختار پوشه‌های شماره‌دار `00-Overview … 05-Data` + `_templates` + `scripts` (+ `06-Analysis` و `07-Exports` در زمان اجرا).
- قراردادهای `00-Overview/conventions.md`: دقت `Decimal` و تلورانس < 0.0001٪، تاکسونومی روند ۷ دسته اصلی + ۳ فرعی، فرمول نمره ترکیبی ۶ وزنی با قید Σw=1، MOC در هر پوشه، frontmatter استاندارد، قراردادهای git (`type(scope): subject`).
- state tracking چهارگانه: `STATUS.md`، `progress.md`، `issues.md`، `decisions.md` (+ placeholderهای `closure.md` و `qa-report.md`).
- درس‌آموخته‌های استخراج‌شده در [`00-Overview/lessons.md`](00-Overview/lessons.md).

## ۳. تطبیق‌های انجام‌شده برای بازار روسیه (تفاوت‌های عمدی با مرجع)

1. **تقویم میلادی**: برخلاف مرجع (شمسی)، همه کلیدهای سال در CSV و تحلیل میلادی است (2021–2025)؛ چون منابع بین‌المللی (BAC/Comtrade) میلادی‌اند. نگاشت شمسی ۱۴۰۰–۱۴۰۴ ≈ 2021–2025 فقط برای گزارش فارسی.
2. **کدهای کشور**: M49 (ایران=۳۶۴، روسیه=۶۴۳) + ISO3 (IRN/RUS) — مرجع از ISO alpha-2 استفاده می‌کرد چون منبعش گمرک ایران بود.
3. **راهبرد داده آینه‌ای**: روسیه از آوریل ۲۰۲۲ آمار گمرکی تفصیلی منتشر نمی‌کند → داده از گزارش‌دهی شرکا (mirror) + IRICA (گمرک ایران) + منابع ثانویه روسی. تنظیم CIF→FOB با ضریب κ ≈ 4–5%.
4. **واحد پول**: داده تجاری دلاری (native). داده روبلی → USD با نرخ میانگین سالانه CBR. داده‌های Rosstat در واحدهای فیزیکی باقی می‌مانند.
5. **فرمول نمره‌دهی**: ساختار ۶ وزنی مرجع حفظ شده؛ در تطبیق روسیه، وزن ششم به‌صورت «ضریب اصطکاک تحریمی» (sanctions friction) بازتعریف می‌شود (مقادیر وزن‌ها TBD در R1).

## ۴. وضعیت و محدودیت‌های فعلی

- همه فایل‌ها `status: draft` — این یک **اسکلت** است، نه تحلیل.
- تسک‌های `R1–R5` در `01-Tasks/` placeholder هستند و پس از پاسخ کاربر به سؤالات شفاف‌سازی، در R1 نهایی می‌شوند.
- پوشه‌های `06-Analysis/` و `07-Exports/` هنوز ساخته نشده‌اند (در R4).
- داده‌ای دانلود یا commit نشده است — `05-Data/raw/` خالی و gitignored است.

## ۵. یادداشت یکپارچگی پرامپت مأموریت

پرامپت bootstrap در حین انتقال دچار خرابی (bit-rot) شده بود؛ مأموریت از روی قطعات تکرارشوندهٔ سازگار (redundancy/fec-style consensus) بازسازی شد. اگر تناقضی میان این سند و درخواست بعدی کاربر دیدید، **درخواست مستقیم کاربر اولویت دارد** و باید در `04-State/decisions.md` ثبت شود.

## ۶. یادآوری امنیتی

توکن دسترسی صادرشده برای این مأموریت فقط در URL ریموت git استفاده شد (نه در هیچ فایلی). پس از پایان کار، کاربر باید آن را در `https://github.com/settings/tokens` revoke کند. (میراث Issue-001 مرجع.)
